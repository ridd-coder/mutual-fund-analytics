nav_history["nav"] = nav_history["nav"].astype(float)

returns = nav_history.groupby(
    "scheme_code"
)["nav"].pct_change()

nav_history["daily_return"] = returns

nav_history.to_csv(
    "data/raw/returns.csv",
    index=False
)